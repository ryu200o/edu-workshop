/**
 * Outbound ports (SPI) owned by the IAM module. All gateways the application calls to reach the
 * outside world (persistence state gateways, query projection gateways, etc.) are declared here.
 * Outbound adapters implement these interfaces.
 */
package io.github.ryu200o.eduworkshop.iam.internal.application.port.outbound;
