package io.github.ryu200o.eduworkshop.attendance.internal.facade;

import io.github.ryu200o.eduworkshop.attendance.AttendanceExposeAPI;

/**
 * Package-private implementation of {@link AttendanceExposeAPI} — the Module Facade for attendance.
 * Resides inside the information-hiding boundary (internal/facade/). Coordinates directly
 * with application ports — no Command/Query Bus involved (per ADR 0010).
 */
class AttendanceExposeAPIImpl implements AttendanceExposeAPI {
}
